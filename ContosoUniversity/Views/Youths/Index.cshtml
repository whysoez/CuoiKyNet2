@model IEnumerable<ContosoUniversity.Models.Youth>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.YouthName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.YouthName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.YouthId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.YouthId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.YouthId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
